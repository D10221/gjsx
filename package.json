{
  "name": "gjsx",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.14.43",
    "prettier": "^2.6.2",
    "typescript": "^4.7.3"
  },
  "scripts": {
    "build": "rm dist -rf && ./build 'app/main.tsx'",
    "watch": "yarn build --watch",
    "start": "gjs dist/app.js",
    "format": "prettier -w ./"
  },
  "dependencies": {
    "@ci-types/gjs": "https://github.com/Gr3q/types-gjs"
  }
}
