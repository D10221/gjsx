{
  "name": "gjsx",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.14.43",
    "prettier": "^2.6.2",
    "typescript": "^4.7.3"
  },
  "scripts": {
    "build": "esbuild app/main.tsx --bundle --outfile=build/app.js",
    "watch": "yarn build --watch",
    "start": "gjs build/app.js",
    "format": "prettier -w ./"
  },
  "dependencies": {
    "@ci-types/gjs": "https://github.com/Gr3q/types-gjs"
  }
}
